VIEW "PURCHASE"
AS SELECT 
u.NAME AS UserName,
p.ID AS PurchaseOrderID,
p.PRODUCT_NAME,
p.PRODUCT_DESC,
p.PRODUCT_SER,
p.PURCHASE_DATE

FROM 
APP_DB_PURCHASE_ORDERS p
INNER JOIN 
APP_DB_USER_DATA u
ON 
p.ID = u.ID; 